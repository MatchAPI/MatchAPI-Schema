# Documentation Source

The source files for the GitHub Wiki live in:

```text
docs/wiki/
```

The Wiki reflects the latest documentation synced from the main branch. For documentation matching an older MatchAPI schema release, browse the corresponding repository tag and read the files under `docs/wiki/`.

